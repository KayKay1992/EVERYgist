================================================================================
                  COMMENT SECTION IMPLEMENTATION DOCUMENTATION
================================================================================

TABLE OF CONTENTS
─────────────────────────────────────────────────────────────────────────────
1. Overview & Architecture
2. Component Hierarchy
3. State Management
4. API Integration
5. Comment Submission Flow
6. Nested Replies System
7. CommentInfo Component
8. CommentReplyInput Component
9. UI/UX Features
10. Validation & Error Handling
11. Real-time Updates
12. Styling & Animations
13. Security Considerations
14. Performance Optimizations
15. Future Enhancements

================================================================================
1. OVERVIEW & ARCHITECTURE
================================================================================

The comment section is a comprehensive, feature-rich system built with React
that enables users to:
  • Post top-level comments on blog posts
  • Reply to existing comments (nested replies)
  • View comment threads with visual hierarchy
  • Generate AI-powered replies (optional)
  • See real-time comment counts and updates

ARCHITECTURAL PATTERN:
  ┌─────────────────────┐
  │  BlogpostView.jsx   │  ← Main Container
  └──────────┬──────────┘
             │
             ├─── CommentReplyInput.jsx  ← Input Component
             │
             └─── CommentInfo.jsx        ← Comment Display (Recursive)
                       │
                       └─── CommentInfo.jsx (Nested Replies)

================================================================================
2. COMPONENT HIERARCHY
================================================================================

BlogpostView (Parent)
├── Comment Section Header
│   ├── Comment count display with icon
│   └── "Add Comment" button
│
├── CommentReplyInput (Top-level)
│   ├── User avatar
│   ├── Text input field
│   ├── AI Generate button (optional)
│   └── Post/Cancel buttons
│
└── Comments List
    └── CommentInfo (Recursive)
        ├── Avatar with gradient ring
        ├── Author name & timestamp
        ├── Comment content
        ├── Action buttons (Reply, View Replies)
        ├── CommentReplyInput (Nested)
        └── Sub-replies (Recursive CommentInfo)

================================================================================
3. STATE MANAGEMENT
================================================================================

BLOGPOSTVIEW STATE:
─────────────────────────────────────────────────────────────────────────────
• blogPostData: null          → Current blog post object
• comments: null              → Array of top-level comments with nested replies
• replyText: ""               → Text for new top-level comment
• isShowReplyForm: false      → Toggle visibility of main comment form

COMMENTINFO STATE (Per Component Instance):
─────────────────────────────────────────────────────────────────────────────
• replyText: ""               → Text for nested reply
• showReplyForm: false        → Toggle reply input form
• showSubReplies: false       → Toggle visibility of nested replies

STATE INITIALIZATION:
```javascript
const [blogPostData, setBlogPostData] = useState(null);
const [comments, setComments] = useState(null);
const [replyText, setReplyText] = useState("");
const [isShowReplyForm, setIsShowReplyForm] = useState(false);
```

================================================================================
4. API INTEGRATION
================================================================================

FETCH COMMENTS:
─────────────────────────────────────────────────────────────────────────────
Endpoint: GET /api/comments/:postId
Response Structure:
```javascript
[
  {
    _id: "comment_id",
    content: "Comment text",
    author: { name: "User", profileImageUrl: "url" },
    post: { _id: "post_id", title: "Post Title" },
    createdAt: "2025-11-20T...",
    updatedAt: "2025-11-20T...",
    replies: [
      {
        _id: "reply_id",
        content: "Reply text",
        author: { name: "User", profileImageUrl: "url" },
        parentComment: "comment_id",
        replies: []
      }
    ]
  }
]
```

Backend Nesting Logic:
```javascript
// Backend creates nested structure
const commentMap = {};
comments.forEach(comment => {
  comment.replies = [];
  commentMap[comment._id] = comment;
});

// Nest replies under parents
comments.forEach(comment => {
  if (comment.parentComment) {
    const parent = commentMap[comment.parentComment];
    if (parent) {
      parent.replies.push(commentMap[comment._id]);
    }
  }
});
```

ADD COMMENT/REPLY:
─────────────────────────────────────────────────────────────────────────────
Endpoint: POST /api/comments/:postId
Request Body:
```javascript
{
  content: "Comment text",
  parentComment: "parent_comment_id" // null for top-level comments
}
```

Backend Processing:
```javascript
const comment = await Comment.create({
  post: postId,
  author: req.user._id,
  content,
  parentComment: parentComment || null,
});
```

================================================================================
5. COMMENT SUBMISSION FLOW
================================================================================

TOP-LEVEL COMMENT SUBMISSION:
─────────────────────────────────────────────────────────────────────────────
1. User clicks "Add Comment" button
   → setIsShowReplyForm(true)

2. User types comment in CommentReplyInput
   → Input onChange: setReplyText(e.target.value)

3. User clicks "Post" button
   → handleAddReply() called

4. Validation:
   ```javascript
   if (!replyText || !replyText.trim()) {
     toast.error("Comment cannot be empty");
     return;
   }
   ```

5. API Request:
   ```javascript
   await axiosInstance.post(
     API_PATHS.COMMENTS.ADD(blogPostData._id),
     { content: replyText }
   );
   ```

6. Success Handling:
   • Show success toast
   • Hide reply form
   • Clear input: setReplyText("")
   • Refresh comments: fetchCommentByPostId()

NESTED REPLY SUBMISSION:
─────────────────────────────────────────────────────────────────────────────
1. User clicks "Reply" on existing comment
   → setShowReplyForm(true) in CommentInfo

2. User types reply
   → setReplyText(value) in CommentInfo state

3. User clicks "Reply" button
   → handleAddReply() in CommentInfo

4. API Request includes parent:
   ```javascript
   await axiosInstance.post(
     API_PATHS.COMMENTS.ADD(post._id),
     {
       content: replyText,
       parentComment: commentId  // ← Links to parent
     }
   );
   ```

5. Success Handling:
   • Show success toast
   • Hide reply form
   • Auto-expand replies: setShowSubReplies(true)
   • Refresh all comments

================================================================================
6. NESTED REPLIES SYSTEM
================================================================================

RECURSIVE COMPONENT PATTERN:
─────────────────────────────────────────────────────────────────────────────
CommentInfo renders itself recursively for nested replies:

```javascript
{showSubReplies && replies?.length > 0 && (
  <div className="mt-4 space-y-4">
    {replies.map((comment) => (
      <CommentInfo
        commentId={comment._id}
        content={comment.content}
        replies={comment.replies || []}
        isSubReply={true}  // ← Prevents infinite nesting
        {...otherProps}
      />
    ))}
  </div>
)}
```

NESTING DEPTH CONTROL:
─────────────────────────────────────────────────────────────────────────────
• isSubReply prop prevents nested replies from having "Reply" button
• Only top-level comments can receive replies
• Backend allows unlimited depth, frontend shows all levels

VISUAL HIERARCHY:
─────────────────────────────────────────────────────────────────────────────
• Top-level: Full width, no indentation
• Nested: ml-8 (2rem left margin)
• Connection lines: Gradient vertical line from parent to children

```javascript
{!isSubReply && (
  <button onClick={() => setShowReplyForm(!showReplyForm)}>
    Reply
  </button>
)}
```

================================================================================
7. COMMENTINFO COMPONENT
================================================================================

COMPONENT SIGNATURE:
─────────────────────────────────────────────────────────────────────────────
```javascript
const CommentInfo = ({
  commentId,        // Unique comment ID
  authorName,       // Comment author's name
  authorPhoto,      // Author's profile picture URL
  content,          // Comment text
  updatedOn,        // Formatted date string
  post,             // Post object (for reply API)
  replies,          // Array of nested replies
  getAllComments,   // Function to refresh comments
  onDelete,         // Delete handler
  isSubReply,       // Boolean for nested comments
}) => { ... }
```

KEY FEATURES:
─────────────────────────────────────────────────────────────────────────────
✓ Premium card design with gradient hover effects
✓ Avatar with gradient ring and glow animation
✓ Reply button with icon rotation on hover
✓ View Replies button with badge counter
✓ Collapsible nested replies with smooth animations
✓ Visual connection lines for reply threads
✓ Authentication check before allowing replies

REPLY BUTTON LOGIC:
─────────────────────────────────────────────────────────────────────────────
```javascript
onClick={() => {
  if (!user) {
    setOpenAuthForm(true);  // Prompt login
    return;
  }
  setShowReplyForm(!showReplyForm);
}}
```

VIEW REPLIES BUTTON:
─────────────────────────────────────────────────────────────────────────────
```javascript
{replies?.length > 0 && (
  <button onClick={() => setShowSubReplies(!showSubReplies)}>
    <span className="badge">{replies.length}</span>
    {replies.length === 1 ? "Reply" : "Replies"}
    <LuChevronDown className={showSubReplies ? "rotate-180" : ""} />
  </button>
)}
```

================================================================================
8. COMMENTREPLYINPUT COMPONENT
================================================================================

COMPONENT SIGNATURE:
─────────────────────────────────────────────────────────────────────────────
```javascript
const CommentReplyInput = ({
  user,               // Current user object
  authorName,         // Original comment author (for @mention)
  content,            // Original comment content (for AI context)
  replyText,          // Controlled input value
  setReplyText,       // State setter from parent
  handleAddReply,     // Submit handler from parent
  handleCancelReply,  // Cancel handler from parent
  disableAutoGen,     // Hide AI generate button
  type = "reply",     // "reply" or "new" (changes UI labels)
}) => { ... }
```

DUAL MODE OPERATION:
─────────────────────────────────────────────────────────────────────────────
1. NEW COMMENT MODE (type="new"):
   • Label: Shows author name
   • Placeholder: "Share your thoughts... 💭"
   • Button: "Post" with send icon
   • No AI Generate (disableAutoGen=true)

2. REPLY MODE (type="reply"):
   • Label: "Reply to @{authorName}"
   • Placeholder: "Write your reply... 💬"
   • Button: "Reply" with reply icon
   • AI Generate available (if enabled)

INPUT FIELD INTEGRATION:
─────────────────────────────────────────────────────────────────────────────
Uses custom Input component with proper event handling:
```javascript
<Input
  value={replyText}
  onChange={(e) => setReplyText(e.target.value)}  // ← Critical fix
  label={type == "new" ? authorName : `Reply to @${authorName}`}
  placeholder={/* ... */}
  type="text"
/>
```

BUTTON STATES:
─────────────────────────────────────────────────────────────────────────────
Submit Button Disabled When:
• replyText.length === 0
• loading (AI generation in progress)

Styling:
```javascript
className={`${
  replyText?.length == 0 || loading
    ? "opacity-50 cursor-not-allowed"
    : "hover:from-sky-600 hover:to-sky-700 hover:scale-105"
}`}
```

AI GENERATE FEATURE:
─────────────────────────────────────────────────────────────────────────────
```javascript
const generateReply = async () => {
  setLoading(true);
  const aiResponse = await axiosInstance.post(
    API_PATHS.AI.GENERATE_COMMENT_REPLY,
    {
      author: { name: authorName },
      content,
    }
  );
  if (aiResponse.data?.length > 0) {
    setReplyText(aiResponse.data);
  }
  setLoading(false);
};
```

================================================================================
9. UI/UX FEATURES
================================================================================

VISUAL DESIGN SYSTEM:
─────────────────────────────────────────────────────────────────────────────
Color Scheme: Purple-Pink-Rose gradient theme
• Purple-600 (#9333EA)
• Pink-600 (#DB2777)
• Rose-600 (#E11D48)
• Sky-500/600 for accents

GRADIENT HEADER:
─────────────────────────────────────────────────────────────────────────────
```javascript
<div className="bg-linear-to-r from-purple-600 via-pink-600 to-rose-600">
  <svg className="comment-icon">{/* ... */}</svg>
  <h3>Comments ({comments?.length || 0})</h3>
</div>
```

EMPTY STATE:
─────────────────────────────────────────────────────────────────────────────
When no comments exist:
```javascript
{comments?.length === 0 && (
  <div className="text-center py-16">
    <div className="gradient-circle">
      <svg className="illustration">{/* ... */}</svg>
    </div>
    <p className="text-gray-500">No comments yet</p>
    <p className="text-sm text-gray-400">
      Be the first to share your thoughts!
    </p>
  </div>
)}
```

AVATAR STYLING:
─────────────────────────────────────────────────────────────────────────────
Premium avatar with gradient ring glow:
```javascript
<div className="relative">
  {/* Glow effect */}
  <div className="absolute inset-0 bg-linear-to-br from-purple-500 to-pink-500 rounded-full blur opacity-20 group-hover:opacity-40" />
  
  {/* Avatar ring */}
  <div className="ring-2 ring-purple-200 group-hover:ring-purple-400">
    {authorPhoto && <img src={authorPhoto} />}
  </div>
</div>
```

HOVER EFFECTS:
─────────────────────────────────────────────────────────────────────────────
• Comment cards: border-gray-200 → border-purple-200
• Gradient accent bar opacity: 0 → 100
• Avatar ring color change
• Button background transitions
• Icon rotations (Reply: 12°, Chevron: 180°)

ANIMATIONS:
─────────────────────────────────────────────────────────────────────────────
• Reply form: animate-in slide-in-from-top duration-300
• Button hover: scale-105
• Icon hover: rotate-12
• Smooth transitions: transition-all duration-300

RESPONSIVE DESIGN:
─────────────────────────────────────────────────────────────────────────────
• AI Generate button: hidden on mobile (hidden sm:inline)
• Flexible layouts with gap utilities
• Proper spacing: p-6, gap-4, space-y-6
• Text truncation: min-w-0 for comment content

================================================================================
10. VALIDATION & ERROR HANDLING
================================================================================

INPUT VALIDATION:
─────────────────────────────────────────────────────────────────────────────
Empty Comment Check:
```javascript
if (!replyText || !replyText.trim()) {
  toast.error("Comment cannot be empty");
  return;
}
```

Whitespace-only Prevention:
• .trim() removes leading/trailing spaces
• Prevents submission of blank comments

AUTHENTICATION CHECK:
─────────────────────────────────────────────────────────────────────────────
```javascript
if (!user) {
  setOpenAuthForm(true);
  return;
}
```

ERROR HANDLING:
─────────────────────────────────────────────────────────────────────────────
Top-level Comments:
```javascript
try {
  await axiosInstance.post(/* ... */);
  toast.success("Comment added successfully");
} catch (error) {
  console.error("Error adding reply:", error);
  toast.error("Failed to add comment. Please try again.");
}
```

Nested Replies:
```javascript
try {
  await axiosInstance.post(/* ... */);
  toast.success("Reply added successfully");
  setShowSubReplies(true);  // Auto-expand to show new reply
} catch (error) {
  console.error("Error adding reply:", error);
  toast.error("Failed to add reply. Please try again.");
}
```

TOAST NOTIFICATIONS:
─────────────────────────────────────────────────────────────────────────────
Using react-hot-toast:
• toast.error() - Red notification for errors
• toast.success() - Green notification for success
• Auto-dismiss after 3 seconds
• Non-blocking, positioned top-right

================================================================================
11. REAL-TIME UPDATES
================================================================================

COMMENT REFRESH STRATEGY:
─────────────────────────────────────────────────────────────────────────────
After any comment/reply submission:
```javascript
fetchCommentByPostId(blogPostData._id);
```

REFRESH FLOW:
─────────────────────────────────────────────────────────────────────────────
1. User submits comment/reply
2. API call succeeds
3. fetchCommentByPostId() re-fetches all comments
4. Backend rebuilds nested structure
5. setComments() updates state
6. React re-renders CommentInfo components
7. New comment appears in UI

OPTIMISTIC UI UPDATES:
─────────────────────────────────────────────────────────────────────────────
• Form hidden immediately after submit
• Success toast shown before refresh completes
• Input cleared instantly
• Auto-expand replies on successful reply submission

PROP DRILLING FOR REFRESH:
─────────────────────────────────────────────────────────────────────────────
```javascript
<CommentInfo
  getAllComments={() => fetchCommentByPostId(blogPostData._id)}
  {...otherProps}
/>
```

Each CommentInfo can trigger full refresh after nested reply.

================================================================================
12. STYLING & ANIMATIONS
================================================================================

TAILWIND CUSTOM UTILITIES:
─────────────────────────────────────────────────────────────────────────────
Custom gradient backgrounds:
• bg-linear-to-r (instead of bg-gradient-to-r)
• bg-linear-to-b (instead of bg-gradient-to-b)
• bg-linear-to-br (instead of bg-gradient-to-br)

COMMENT CARD STRUCTURE:
─────────────────────────────────────────────────────────────────────────────
```javascript
<div className="group">
  <div className="bg-white rounded-2xl border border-gray-200 hover:border-purple-200 hover:shadow-lg transition-all duration-300">
    {/* Gradient accent bar */}
    <div className="absolute top-0 left-0 w-1 h-full bg-linear-to-b from-purple-500 via-pink-500 to-rose-500 opacity-0 group-hover:opacity-100" />
    
    {/* Content */}
  </div>
</div>
```

CONNECTION LINES:
─────────────────────────────────────────────────────────────────────────────
Visual thread indicator for nested replies:
```javascript
<div className="absolute left-4 top-0 bottom-0 w-0.5 bg-linear-to-b from-purple-200 via-pink-200 to-transparent" />
```

BUTTON STYLES:
─────────────────────────────────────────────────────────────────────────────
Reply Button:
```css
text-gray-600 hover:text-purple-600 hover:bg-purple-50
group-hover/btn:rotate-12 transition-transform
```

View Replies Button:
```css
text-gray-600 hover:text-pink-600 hover:bg-pink-50
badge: bg-linear-to-r from-purple-100 to-pink-100
```

Submit Button:
```css
bg-linear-to-r from-sky-500 to-sky-600
hover:from-sky-600 hover:to-sky-700
hover:scale-105 hover:shadow-lg
```

SPACING SYSTEM:
─────────────────────────────────────────────────────────────────────────────
• Comment padding: p-6
• Gap between elements: gap-4
• Comment spacing: space-y-6
• Nested margin: ml-8 (sub-replies), ml-12 (reply form)

================================================================================
13. SECURITY CONSIDERATIONS
================================================================================

AUTHENTICATION ENFORCEMENT:
─────────────────────────────────────────────────────────────────────────────
Frontend Check:
```javascript
if (!user) {
  setOpenAuthForm(true);
  return;
}
```

Backend Middleware:
• authMiddleware.js protects POST /api/comments/:postId
• req.user populated by JWT verification
• Only authenticated users can comment

XSS PREVENTION:
─────────────────────────────────────────────────────────────────────────────
React automatically escapes text content:
```javascript
<p>{content}</p>  // Safe - React escapes HTML
```

Markdown rendering uses sanitization:
```javascript
import { sanitizeMarkdown } from "../../utils/helper";
const sanitized = sanitizeMarkdown(content);
```

INPUT SANITIZATION:
─────────────────────────────────────────────────────────────────────────────
Backend should implement:
• Content length limits
• HTML tag stripping
• SQL injection prevention (Mongoose handles this)
• Rate limiting on comment endpoints

AUTHORIZATION:
─────────────────────────────────────────────────────────────────────────────
• Users can only delete their own comments
• Backend verifies comment.author === req.user._id
• Frontend onDelete triggers confirmation modal

================================================================================
14. PERFORMANCE OPTIMIZATIONS
================================================================================

CONDITIONAL RENDERING:
─────────────────────────────────────────────────────────────────────────────
Avatar Images:
```javascript
{authorPhoto && (
  <img src={authorPhoto} alt={authorName} />
)}
```

Prevents empty src warnings and unnecessary DOM elements.

MEMOIZATION OPPORTUNITIES:
─────────────────────────────────────────────────────────────────────────────
Consider React.memo for CommentInfo:
```javascript
export default React.memo(CommentInfo, (prev, next) => {
  return prev.commentId === next.commentId &&
         prev.replies.length === next.replies.length;
});
```

LAZY LOADING:
─────────────────────────────────────────────────────────────────────────────
Replies are collapsed by default:
• showSubReplies: false
• Only render nested CommentInfo when expanded
• Reduces initial DOM size

EFFICIENT STATE UPDATES:
─────────────────────────────────────────────────────────────────────────────
Toggle pattern prevents unnecessary re-renders:
```javascript
setShowReplyForm(prevState => !prevState);
```

API CALL OPTIMIZATION:
─────────────────────────────────────────────────────────────────────────────
• Single GET request fetches all comments with nested structure
• Backend does nesting work, not frontend
• Avoid multiple API calls for each reply level

================================================================================
15. FUTURE ENHANCEMENTS
================================================================================

FEATURE ROADMAP:
─────────────────────────────────────────────────────────────────────────────
✓ Implemented:
  • Top-level comments
  • Nested replies
  • AI-generated replies
  • Real-time refresh
  • Premium UI design

⏳ Planned Enhancements:

1. COMMENT EDITING:
   • Edit button for own comments
   • Inline editor with markdown support
   • Edit history tracking
   • "Edited" badge with timestamp

2. COMMENT DELETION:
   • Confirmation modal (already wired)
   • Soft delete vs hard delete
   • Cascade delete for nested replies
   • Restore deleted comments

3. REACTIONS & LIKES:
   • Like/upvote system
   • Emoji reactions (👍❤️😂🎉)
   • Reaction counts
   • User reaction tracking

4. SORTING & FILTERING:
   • Sort by: Newest, Oldest, Most Liked
   • Filter by: All, Unread, Mentions
   • Search within comments

5. PAGINATION:
   • Load more comments (infinite scroll)
   • "Show more replies" for long threads
   • Virtual scrolling for performance

6. NOTIFICATIONS:
   • Email notifications for replies
   • In-app notification badge
   • Real-time updates with WebSocket

7. MENTIONS & TAGGING:
   • @username autocomplete
   • Mention notifications
   • Clickable user tags

8. MARKDOWN SUPPORT:
   • Rich text editor
   • Markdown preview
   • Code highlighting in comments
   • Image uploads

9. MODERATION:
   • Report comment feature
   • Admin moderation panel
   • Spam detection
   • Profanity filter

10. ACCESSIBILITY:
    • ARIA labels
    • Keyboard navigation
    • Screen reader support
    • Focus management

11. ANALYTICS:
    • Comment engagement metrics
    • Average response time
    • User participation rates
    • Popular discussion topics

12. REAL-TIME COLLABORATION:
    • WebSocket integration
    • Live typing indicators
    • Instant comment updates
    • Presence detection

TECHNICAL DEBT:
─────────────────────────────────────────────────────────────────────────────
• Add PropTypes or TypeScript
• Implement error boundaries
• Add unit tests (Jest + React Testing Library)
• E2E tests (Playwright/Cypress)
• Optimize bundle size
• Add loading skeletons for comments
• Implement optimistic UI updates

SCALABILITY CONSIDERATIONS:
─────────────────────────────────────────────────────────────────────────────
• Database indexing on post._id
• Caching strategy (Redis)
• CDN for profile images
• Rate limiting per user
• Comment count limits per post

================================================================================
                              END OF DOCUMENTATION
================================================================================